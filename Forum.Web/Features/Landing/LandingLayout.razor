@* Forum.Web/Features/Landing/LandingLayout.razor *@
@* Full-bleed layout for the marketing landing page. No forum top-bar — the *@
@* landing owns its own fixed glass nav and footer. Deliberately plain (no    *@
@* @rendermode) so the page stays static-SSR and the no-JS fallback holds.    *@
@inherits LayoutComponentBase

@Body
